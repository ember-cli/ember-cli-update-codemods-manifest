name: CI

on:
  # allow manual running
  workflow_dispatch:
  push:
    branches:
    - main
  pull_request:

jobs:
  lint-git:
    if: github.event_name == 'pull_request'

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
      with:
        ref: ${{ github.head_ref }}

    - run: |
        git fetch --prune --unshallow
        git branch main origin/main

    - uses: actions/setup-node@v5
      with:
        node-version: 22

    - uses: pnpm/action-setup@v5.0.0
      with:
        version: 10.34.5

    - run: pnpm install

    - run: npm run lint:git -- --from ${{ github.event.pull_request.base.sha }} --to ${{ github.event.pull_request.head.sha }} --verbose

  lint:
    needs: lint-git

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - uses: actions/setup-node@v5
      with:
        node-version: 22

    - uses: pnpm/action-setup@v5.0.0
      with:
        version: 10.34.5

    - run: pnpm install

    - run: npm run lint

  test:
    needs: lint

    strategy:
      matrix:
        os:
        - ubuntu-latest
        - macos-latest
        node:
        - 22

        - '*'
        exclude:
        - os: macos-latest
          node: '*'

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v5

    - uses: actions/setup-node@v5
      with:
        node-version: ${{ matrix.node }}

    - uses: pnpm/action-setup@v5.0.0
      with:
        version: 10.34.5

    - run: pnpm install

    - run: npm test
      continue-on-error: ${{ matrix.os == 'macos-latest' }}
      env:
        TEST_TYPE: addon
        DEBUG: ember-cli-update,boilerplate-update,git-diff-apply

  ember-cli-update:
    needs: test
    if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
      with:
        ref: ${{ github.head_ref }}
        token: ${{ secrets.GitHubToken }}

    - uses: actions/setup-node@v5
      with:
        node-version: 22

    - uses: pnpm/action-setup@v5.0.0
      with:
        version: 10.34.5

    - uses: kellyselden/ember-cli-update-action@v7
      with:
        autofix_command: npm run lint:js -- --fix
        ignore_to: true
        commit_prefix: "chore: "
